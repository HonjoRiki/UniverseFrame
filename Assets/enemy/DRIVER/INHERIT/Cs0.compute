#version 450
#pragma kernel main

GLSLPROGRAM

layout(std430, binding = 0)buffer Result { float results[]; };

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1)in;

void main()
{
	results[gl_LocalInvocationID.x] = 11.0;
}

ENDGLSL